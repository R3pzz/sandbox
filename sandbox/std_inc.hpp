#pragma once

/* windows api: entry point, memory */
#include <windows.h>
/* console api: console */
#include <consoleapi2.h>
/* input handling */
#include <winuser.h>
/* directories */
#include <shlobj.h>

/* streams: ofstream, ifstream, ostream, istream, cin, cout */
#include <iostream>
#include <fstream>
/* file ops */
#include <filesystem>
/* stringstream */
#include <sstream>

/* multithreading: mutex, semaphore, unique_lock, binary_semaphore... */
#include <mutex>
#include <semaphore>
/* thread ops: this_thread */
#include <thread>

/* timing */
#include <ctime>
/* chrono */
#include <chrono>

/* math */
#include <cmath>

/* datatypes: array, vector, map, unordered_map... */
#include <vector>
#include <array>
#include <unordered_map>
#include <map>